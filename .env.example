# StepInTheRing — environment (server-side unless prefixed NEXT_PUBLIC_)

# Etsy Open API v3 (Design Shop Engine publishing — not yet connected).
# SERVER ONLY. Never expose these to the client or commit real values.
ETSY_SHOP_ID=
ETSY_API_KEY=
ETSY_SHARED_SECRET=
ETSY_REDIRECT_URI=
ETSY_ACCESS_TOKEN=
ETSY_REFRESH_TOKEN=

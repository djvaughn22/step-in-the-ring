# Owner’s Builds cross-link follow-up

Canonical destination:

`https://stepinthering.com/builds`

This change intentionally edits only Step In The Ring. The sibling repos were
inspected read-only. Their future, non-intrusive placement can be:

- `OpenMirror/crossheartpray`: the quiet site footer or About page. Suggested
  text: `More builds by DJ → Explore Owner’s Builds` linking to the canonical
  destination.
- `TheDJCares/thedjcares`: the About page or site footer, using the same exact
  text and URL.
- `WatchedNotWatched/watched-not-watched`: the About page or site footer, using
  the same exact text and URL.
- `idontcry`: the About page or family footer, using the same exact text and
  URL. This checkout had active uncommitted Sports Desk work when inspected,
  so it was deliberately left untouched.

No sibling link is claimed as complete here. Add each one in that repository's
own normal footer/About location when its working tree and active ownership
make that safe.
